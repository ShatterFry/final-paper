#pragma once

#include <vector>

#include <Circle.h>
#include <Rectangle.h>

using UIntVector = std::vector<unsigned int>;
using FloatVector = std::vector<float>;
using CircleVector = std::vector<Circle>;
using RectangleVector = std::vector<Rectangle>;