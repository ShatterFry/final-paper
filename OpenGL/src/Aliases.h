#pragma once

#include <vector>
#include <map>

using UIntVector = std::vector<unsigned int>;
using FloatVector = std::vector<float>;
using AgeRadiusVector = std::vector<float>;
using AgeRadiusMap = std::map<enum class EAgeType, float>;